import {describe,it,expect} from 'vitest';import {calculateIEA,ieaStatus} from '@/lib/iea';
describe('IEA',()=>{it('calcula índice ponderado entre 0 e 100',()=>{const v=calculateIEA({frequency:100,streak:100,trainingMonths:100,events:100,competitions:100,graduationProgress:100,attendance:100});expect(v).toBe(100)});it('classifica risco',()=>{expect(ieaStatus(35)).toBe('Atenção');expect(ieaStatus(85)).toBe('Excelente evolução')})})
