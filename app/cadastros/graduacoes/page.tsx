'use client';import {AppShell} from '@/components/app-shell';import {SimpleCrud} from '@/components/admin/simple-crud';
export default function Page(){return <AppShell><SimpleCrud title="Graduações / Faixas" subtitle="Ordem das faixas e tempo mínimo sugerido para progressão." endpoint="/api/admin/belts" defaults={{minimum_months:0,sort_order:1}} fields={[{name:'name',label:'Faixa',required:true},{name:'sort_order',label:'Ordem',type:'number',required:true},{name:'minimum_months',label:'Tempo mínimo (meses)',type:'number',required:true}]} columns={[{key:'sort_order',label:'Ordem'},{key:'name',label:'Faixa'},{key:'minimum_months',label:'Tempo mínimo',render:r=>`${r.minimum_months||0} meses`} ]}/></AppShell>}
