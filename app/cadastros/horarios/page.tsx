'use client';import {useEffect,useState} from 'react';import {AppShell} from '@/components/app-shell';import {Plus,RefreshCw} from 'lucide-react';
const days=['Domingo','Segunda-feira','Terça-feira','Quarta-feira','Quinta-feira','Sexta-feira','Sábado'];
type Prof={id:string;name:string};
export default function Page(){const[rows,setRows]=useState<any[]>([]);const[prof,setProf]=useState<Prof[]>([]);const[open,setOpen]=useState(false);const[msg,setMsg]=useState('');const[form,setForm]=useState({name:'Jiu-Jitsu Adulto',weekday:1,start_time:'20:30',end_time:'22:00',professor_id:'',capacity:30,active:true});async function load(){const[r,p]=await Promise.all([fetch('/api/admin/schedules',{cache:'no-store'}),fetch('/api/admin/professors',{cache:'no-store'})]);const j=await r.json();const jp=await p.json();if(r.ok)setRows(j);if(p.ok)setProf(jp.map((x:any)=>({id:x.id,name:x.name})));}useEffect(()=>{void load()},[]);async function save(e:React.FormEvent){e.preventDefault();setMsg('Salvando...');const r=await fetch('/api/admin/schedules',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(form)});const j=await r.json();if(!r.ok){setMsg(j.error||'Falha ao salvar');return}setMsg('Horário salvo com sucesso.');setOpen(false);await load()}return <AppShell><section className="hero"><div className="split"><div><h1>Horários</h1><div className="muted">Grade semanal, professor responsável e capacidade padrão.</div></div><button className="btn btn-primary" onClick={()=>setOpen(v=>!v)}><Plus size={16}/> Novo horário</button></div></section>{msg&&<div className={msg.includes('sucesso')?'notice success':'notice error'} style={{marginBottom:14}}>{msg}</div>}{open&&<form className="card admin-form" onSubmit={save}><div className="grid grid-3"><div><label className="label">Aula / turma</label><input className="input" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/></div><div><label className="label">Dia</label><select className="input" value={form.weekday} onChange={e=>setForm({...form,weekday:Number(e.target.value)})}>{days.map((d,i)=><option value={i} key={d}>{d}</option>)}</select></div><div><label className="label">Professor</label><select className="input" value={form.professor_id} onChange={e=>setForm({...form,professor_id:e.target.value})} required><option value="">Selecione...</option>{prof.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></div><div><label className="label">Início</label><input type="time" className="input" value={form.start_time} onChange={e=>setForm({...form,start_time:e.target.value})}/></div><div><label className="label">Fim</label><input type="time" className="input" value={form.end_time} onChange={e=>setForm({...form,end_time:e.target.value})}/></div><div><label className="label">Limite de alunos</label><input type="number" className="input" value={form.capacity} onChange={e=>setForm({...form,capacity:Number(e.target.value)})}/></div></div><button className="btn btn-primary" style={{marginTop:16}}>Salvar horário</button></form>}<div className="card" style={{padding:18}}><div className="split" style={{marginBottom:12}}><strong>{rows.length} horário(s)</strong><button className="btn btn-secondary" onClick={load}><RefreshCw size={14}/> Atualizar</button></div>{!rows.length?<div className="empty-state">Cadastre primeiro um professor e depois monte a grade semanal.</div>:<div className="table-wrap"><table className="table"><thead><tr><th>Dia</th><th>Horário</th><th>Turma</th><th>Professor</th><th>Limite</th><th>Status</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{days[x.weekday]}</td><td>{String(x.start_time).slice(0,5)}–{String(x.end_time).slice(0,5)}</td><td>{x.name}</td><td>{x.profiles?.name||'-'}</td><td>{x.capacity}</td><td>{x.active?'Ativo':'Inativo'}</td></tr>)}</tbody></table></div>}</div></AppShell>}
