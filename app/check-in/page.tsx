'use client';
import {Suspense,useCallback,useEffect,useState} from 'react';import {useSearchParams} from 'next/navigation';import QRCode from 'qrcode';import {AppShell} from '@/components/app-shell';import {QrCode,RefreshCw,MapPin,ShieldCheck} from 'lucide-react';
function CheckInContent(){const sp=useSearchParams();const classId=sp.get('class')||'';const[token,setToken]=useState('');const[src,setSrc]=useState('');const[seconds,setSeconds]=useState(30);const[error,setError]=useState('');const renew=useCallback(async()=>{if(!classId){setError('Abra uma aula e acesse o QR por ela.');return}setError('');const res=await fetch(`/api/classes/${classId}/qr`,{method:'POST'});const data=await res.json();if(!res.ok){setError(data.error||'Não foi possível gerar o QR.');setToken('');return}setToken(data.token);setSeconds(data.ttl||30)},[classId]);useEffect(()=>{void renew()},[renew]);useEffect(()=>{if(!token)return;QRCode.toDataURL(`${location.origin}/check-in/scan?token=${encodeURIComponent(token)}`,{width:300,margin:2}).then(setSrc)},[token]);useEffect(()=>{if(!token)return;const i=setInterval(()=>setSeconds(s=>{if(s<=1){void renew();return 30}return s-1}),1000);return()=>clearInterval(i)},[renew,token]);return <AppShell><section className="hero"><h1>Check-in por QR Code</h1><div className="muted">Código seguro, exclusivo da aula, uso único e renovado a cada 30 segundos.</div></section>{error?<div className="notice error">{error}</div>:<div className="grid grid-2"><div className="card" style={{padding:26,textAlign:'center'}}><span className="pill"><QrCode size={14}/> QR SEGURO</span>{src&&<img src={src} alt="QR Code da aula" width={300} height={300} style={{background:'#fff',borderRadius:16,maxWidth:'100%',marginTop:20}}/>}<h3>Expira em {seconds}s</h3><div className="progress"><span style={{width:`${seconds/30*100}%`}}/></div><button className="btn btn-secondary" style={{marginTop:18}} onClick={()=>void renew()}><RefreshCw size={15}/> Renovar agora</button></div><div className="card" style={{padding:24}}><h2>Segurança do check-in</h2><p><ShieldCheck size={17} style={{verticalAlign:'middle'}}/> Token aleatório armazenado apenas como hash.</p><p><RefreshCw size={17} style={{verticalAlign:'middle'}}/> Expiração de 30 segundos e uso único.</p><p><MapPin size={17} style={{verticalAlign:'middle'}}/> IP, dispositivo e geolocalização quando autorizada.</p><p className="muted">Não existe mais modo demonstração: o QR só é exibido quando o Supabase valida a aula e o usuário.</p></div></div>}</AppShell>}
export default function Page(){return <Suspense fallback={<div className="empty">Carregando QR...</div>}><CheckInContent/></Suspense>}
