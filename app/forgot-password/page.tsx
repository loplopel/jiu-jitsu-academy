'use client';
import { useState } from 'react'; import Link from 'next/link'; import { getSupabaseBrowserClient } from '@/lib/supabase-browser';
export default function Forgot(){const[email,setEmail]=useState('');const[msg,setMsg]=useState('');const[error,setError]=useState('');async function send(e:React.FormEvent){e.preventDefault();setError('');const sb=getSupabaseBrowserClient();if(!sb){setError('Configure o Supabase primeiro.');return;}const redirectTo=`${window.location.origin}/update-password`;const {error}=await sb.auth.resetPasswordForEmail(email,{redirectTo});if(error)setError(error.message);else setMsg('Enviamos o link de recuperação para o seu e-mail.');}return <main className="auth-panel" style={{minHeight:'100vh'}}><div className="card auth-box"><h2>Recuperar senha</h2><p className="muted">Informe seu e-mail cadastrado.</p><form className="form-stack" onSubmit={send}><input className="input" type="email" placeholder="seu@email.com" value={email} onChange={e=>setEmail(e.target.value)} required/>{error&&<div className="notice error">{error}</div>}{msg&&<div className="notice success">{msg}</div>}<button className="btn btn-primary">Enviar link</button><Link href="/login" style={{textAlign:'center',fontSize:13}}>Voltar ao login</Link></form></div></main>}
