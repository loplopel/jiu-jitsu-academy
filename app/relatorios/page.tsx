'use client';import {AppShell} from '@/components/app-shell';import {exportPDF,exportExcel,exportCSV} from '@/lib/export';
const rows=[{Tipo:'Presença',Período:'Agosto/2026',Registros:463},{Tipo:'Mensalidade',Período:'Agosto/2026',Registros:284},{Tipo:'Graduação',Período:'2026',Registros:61},{Tipo:'Financeiro',Período:'Agosto/2026',Registros:284}];
export default function Page(){return <AppShell><section className="hero"><h1>Central de relatórios</h1><div className="muted">Presença, mensalidade, graduação, professor, aluno e financeiro.</div></section><div className="grid grid-3">{['Presença','Mensalidade','Graduação','Professor','Aluno','Financeiro'].map(x=><div className="card stat" key={x}><span className="pill">RELATÓRIO</span><h2>{x}</h2><p className="muted">Filtros por período, status e responsável.</p><div className="toolbar"><button className="btn btn-secondary" onClick={()=>exportPDF(rows,`relatorio-${x.toLowerCase()}`)}>PDF</button><button className="btn btn-secondary" onClick={()=>exportExcel(rows,`relatorio-${x.toLowerCase()}`)}>Excel</button><button className="btn btn-secondary" onClick={()=>exportCSV(rows,`relatorio-${x.toLowerCase()}`)}>CSV</button></div></div>)}</div></AppShell>}
