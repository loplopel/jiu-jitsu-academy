'use client';
import {useState} from 'react'; import {Plus,Search,Download} from 'lucide-react'; import {exportCSV,exportExcel,exportPDF} from '@/lib/export';
export function DataPage({title,subtitle,columns,seed,formFields}:{title:string;subtitle:string;columns:string[];seed:Record<string,unknown>[];formFields:string[]}){const[rows,setRows]=useState(seed);const[q,setQ]=useState('');const[open,setOpen]=useState(false);const[form,setForm]=useState<Record<string,string>>({});const filtered=rows.filter(r=>JSON.stringify(r).toLowerCase().includes(q.toLowerCase()));function add(e:React.FormEvent){e.preventDefault();setRows([{id:crypto.randomUUID(),...form},...rows]);setForm({});setOpen(false)}return <><section className="hero"><div className="split"><div><h1>{title}</h1><div className="muted">{subtitle}</div></div><button className="btn btn-primary" onClick={()=>setOpen(!open)}><Plus size={16}/> Novo</button></div></section>{open&&<form className="card" style={{padding:18,marginBottom:18}} onSubmit={add}><div className="grid grid-3">{formFields.map(f=><div key={f}><label className="label">{f}</label><input className="input" value={form[f]||''} onChange={e=>setForm({...form,[f]:e.target.value})} required={f==='Nome'}/></div>)}</div><div className="toolbar" style={{marginTop:14}}><button className="btn btn-primary">Salvar</button><button type="button" className="btn btn-secondary" onClick={()=>setOpen(false)}>Cancelar</button></div></form>}<div className="card" style={{padding:18}}><div className="split" style={{marginBottom:14}}><div style={{position:'relative',minWidth:280}}><Search size={16} style={{position:'absolute',left:12,top:13,color:'#97a1b2'}}/><input className="input" style={{paddingLeft:36}} placeholder="Buscar..." value={q} onChange={e=>setQ(e.target.value)}/></div><div className="toolbar"><button className="btn btn-secondary" onClick={()=>exportPDF(filtered,'relatorio')}><Download size={14}/> PDF</button><button className="btn btn-secondary" onClick={()=>exportExcel(filtered,'relatorio')}>Excel</button><button className="btn btn-secondary" onClick={()=>exportCSV(filtered,'relatorio')}>CSV</button></div></div><div className="table-wrap"><table className="table"><thead><tr>{columns.map(c=><th key={c}>{c}</th>)}</tr></thead><tbody>{filtered.map((r,i)=><tr key={String(r.id||i)}>{columns.map(c=><td key={c}>{String(r[c]??'-')}</td>)}</tr>)}</tbody></table></div>{!filtered.length&&<div className="empty-state" style={{marginTop:12}}>Nenhum registro cadastrado ainda.</div>}</div></>}
