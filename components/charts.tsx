'use client';
import {ResponsiveContainer,AreaChart,Area,XAxis,YAxis,Tooltip,BarChart,Bar} from 'recharts';
export type ChartPoint={label:string;value:number};
export function MonthlyChart({data}:{data:ChartPoint[]}){if(!data.some(x=>x.value>0))return <div className="empty-state">O gráfico aparecerá após os primeiros check-ins.</div>;return <div style={{height:260}}><ResponsiveContainer width="100%" height="100%"><AreaChart data={data}><XAxis dataKey="label"/><YAxis allowDecimals={false}/><Tooltip/><Area dataKey="value" stroke="#f97316" fill="#f9731633"/></AreaChart></ResponsiveContainer></div>}
export function OccupancyChart({data}:{data:ChartPoint[]}){if(!data.some(x=>x.value>0))return <div className="empty-state">A ocupação aparecerá quando houver aulas e reservas.</div>;return <div style={{height:260}}><ResponsiveContainer width="100%" height="100%"><BarChart data={data}><XAxis dataKey="label"/><YAxis/><Tooltip/><Bar dataKey="value" fill="#f97316" radius={[5,5,0,0]}/></BarChart></ResponsiveContainer></div>}
